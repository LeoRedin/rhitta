# Used by apps/web's server-side handlers and Better Auth React client.
# Both URLs point at apps/api — web reuses the API's Better Auth endpoints at
# the API origin, so no separate web-side auth secret is needed.
VITE_API_URL=http://localhost:4000
VITE_BETTER_AUTH_URL=http://localhost:4000
